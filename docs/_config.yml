theme: jekyll-theme-hacker
title: Agentic Salesforce Dev Docs
description: Notes and guides for agentic Salesforce development with CumulusCI and GitHub Copilot

# Apply the Hacker theme default layout to every page automatically
# so individual markdown files don't need explicit front matter
defaults:
  - scope:
      path: ""
    values:
      layout: "default"

